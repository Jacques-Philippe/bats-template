name: Unit Tests
on: push

jobs:
  run_tests:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0
        with: 
          submodules: 'recursive'
      - name: "Execute tests"
        run: bash ./run-bats.sh
        
